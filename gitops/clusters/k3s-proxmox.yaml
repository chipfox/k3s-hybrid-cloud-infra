apiVersion: v1
kind: Secret
metadata:
  name: k3s-proxmox
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: cluster
    environment: production
    cluster: k3s
type: Opaque
stringData:
  name: k3s-proxmox
  server: https://10.0.0.230:6443
  config: "{}"
  # For advanced configuration (TLS, auth), see docs/CONFIG-EXAMPLES.md
