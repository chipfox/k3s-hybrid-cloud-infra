apiVersion: v1
kind: Secret
metadata:
  name: eks-aws
  namespace: argocd
  labels:
    argocd.argoproj.io/secret-type: cluster
    environment: production
    cluster: eks
type: Opaque
stringData:
  name: eks-aws
  server: https://<eks-cluster-endpoint>
  config: "{}"
  # For advanced configuration (AWS auth, TLS), see docs/CONFIG-EXAMPLES.md
